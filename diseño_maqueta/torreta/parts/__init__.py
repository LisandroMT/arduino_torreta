"""Subconjuntos de la torreta. Cada módulo expone build(cfg) -> list[(Part, rol)].

El 'rol' (body / tube / accent) determina el color en el render; no afecta la
impresión, donde todo se fusiona en un único sólido.
"""
